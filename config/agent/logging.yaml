# -*- coding: utf-8 -*-
# 日志配置（支持热重载，动态调整日志级别）

logging:
  level: "INFO"
  format: "%(asctime)s [%(levelname)s] %(name)s: %(message)s"
  console: true
  file: "logs/cellium.log"
  max_size: 5000
  backup_count: 5
  client_log: false
