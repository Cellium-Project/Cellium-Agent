llm:
  section: llm
  provider: openai
  streaming:
    enabled: true
  models:
  - name: model-1
    api_key: ${LLM_MODELS_API_KEY}
    base_url: ${LLM_MODELS_BASE_URL}
    model: ${LLM_MODELS_MODEL}
    temperature: 0.7
    timeout: 120
  current_model: model-1
  openai:
    api_key: ${LLM_MODELS_API_KEY}
    base_url: ${LLM_MODELS_BASE_URL}
    model: ${LLM_MODELS_MODEL}
    temperature: 0.7
    timeout: 120
