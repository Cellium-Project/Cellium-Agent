# ============================================================
# 组件配置 — 由系统自动维护
#
# 【组件使用铁律LLM必读】
# 1. 继承 BaseCell，定义 cell_name 和 execute()
# 2. 命令方法以 _cmd_ 前缀命名，必须写 docstring
# 3. 文件放 components/ 下，系统自动发现并注册
# 4. 不要手动修改 enabled_components 列表（系统自动生成）
# 5. 卸载组件只需删除对应的 .py 文件
#
# 启用的组件列表（系统自动生成，请勿手动编辑）
# ============================================================
enabled_components:
- components.component_builder.ComponentBuilder
- components.skill_installer.SkillInstaller
- components.web_fetch.WebFetch
- components.web_search.WebSearch
