# Python Cellium Agent - 依赖包

# Web 框架
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
python-multipart>=0.0.27

# 配置文件解析
pyyaml>=6.0.3

# 中文分词（用于记忆检索增强）
jieba>=0.42.1

# 中文拼音（用于向量检索增强，可选）
pypinyin>=0.50.0

# 浏览器控制（用于网页搜索和抓取）
DrissionPage>=4.1.1.2

# OpenAI 兼容客户端（LLM 引擎依赖）
openai>=1.0.0

# QQ Bot WebSocket 客户端
websockets>=12.0

# HTTP 客户端（用于 QQ 和 Telegram 文件上传）
httpx>=0.25.0

# 注意：其他功能使用 Python 标准库，无需额外依赖
# - sqlite3: FTS5 全文检索（Python 标准库）
# - subprocess: 命令执行（Python 标准库）
# - json: JSON 序列化（Python 标准库）
# - re/difflib: 文本处理（Python 标准库）
